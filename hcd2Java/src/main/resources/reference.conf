// Akka config file

akka {
  log-dead-letters = 0
  loglevel = "DEBUG"
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  stdout-loglevel = OFF

  actor {
    provider = remote
  }
  remote {
    log-remote-lifecycle-events = off
    artery {
      enabled = on
      canonical.port = 0
    }
  }
}
